package com.sourav.chitchat.ui.details.data

class ChatDetailsRepository constructor(
    private val remoteSource: ChatDetailsRemoteDataSource
) {
   /* fun loginSendOtp(number: String, countryCode: String, method: String) = networkLiveData(
        networkCall = {
            remoteSource.loginSendOtp("")
        }
    ).distinctUntilChanged()*/
}
